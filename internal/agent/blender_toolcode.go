// Code generated from the Blender MCP reference tool-code. DO NOT EDIT.
//
// Source: https://projects.blender.org/lab/blender_mcp (mcp/blmcp/tools/*_toolcode.py)
// The bodies are embedded verbatim so the built-in Blender transport executes exactly the
// same Blender-side Python the official bridge sends. Regenerate with
// internal/agent/testdata/gen_blender_toolcode.py.

package agent

// blenderToolCodeGetObjectsSummary is the Blender-side program for the get_objects_summary tool.
const blenderToolCodeGetObjectsSummary = "__all__ = (\n    \"Result\",\n    \"main\",\n)\n\nfrom typing import Any, NamedTuple\n\n# Placeholder types for Blender Python objects (no stubs available).\n_Object = Any\n_Collection = Any\n_LayerCollection = Any\n\n\nclass Result(NamedTuple):\n    status: str\n    scene_name: str\n    active_workspace: str | None\n    active_object: str | None\n    object_mode: str | None\n    camera_object: str | None\n    collections: list[dict[str, Any]]\n\n\ndef _object_info(obj: _Object) -> dict[str, Any]:\n    info: dict[str, Any] = {\n        \"name\": obj.name,\n        \"type\": obj.type,\n        \"parent\": obj.parent.name if obj.parent else None,\n        \"data_name\": obj.data.name if obj.data else None,\n        \"selected\": obj.select_get(),\n        \"visible\": obj.visible_get(),\n        \"hide_viewport\": obj.hide_get(),\n    }\n    if obj.type == 'EMPTY' and obj.instance_type == 'COLLECTION' and obj.instance_collection is not None:\n        info[\"instance_collection\"] = obj.instance_collection.name\n    return info\n\n\ndef _layer_collection_tree(lc: _LayerCollection) -> dict[str, Any]:\n    col = lc.collection\n    return {\n        \"name\": col.name,\n        \"exclude\": lc.exclude,\n        \"hide_viewport\": col.hide_viewport,\n        \"objects\": sorted(\n            [_object_info(obj) for obj in col.objects],\n            key=lambda o: o[\"name\"],\n        ),\n        \"children\": sorted(\n            [_layer_collection_tree(child) for child in lc.children],\n            key=lambda c: c[\"name\"],\n        ),\n    }\n\n\ndef main(params: None) -> Result:\n    del params\n    from bpy import context  # pylint: disable=import-error,no-name-in-module\n\n    scene = context.scene\n    view_layer = context.view_layer\n\n    root = view_layer.layer_collection\n    collections = sorted(\n        [_layer_collection_tree(root)],\n        key=lambda c: c[\"name\"],\n    )\n\n    active = view_layer.objects.active\n    window = getattr(context, \"window\", None)\n    return Result(\n        status=\"ok\",\n        scene_name=scene.name,\n        active_workspace=window.workspace.name if window else None,\n        active_object=active.name if active else None,\n        object_mode=context.mode if active else None,\n        camera_object=scene.camera.name if scene.camera else None,\n        collections=collections,\n    )"

// blenderToolCodeGetObjectDetailSummary is the Blender-side program for the get_object_detail_summary tool.
const blenderToolCodeGetObjectDetailSummary = "__all__ = (\n    \"Params\",\n    \"Result\",\n    \"main\",\n)\n\nfrom typing import NamedTuple\n\n\nclass Params(NamedTuple):\n    name: str\n\n\nclass Result(NamedTuple):\n    status: str\n    name: str | None = None\n    type: str | None = None\n    location: list[float] | None = None\n    rotation: list[float] | None = None\n    scale: list[float] | None = None\n    dimensions: list[float] | None = None\n    parent: str | None = None\n    children: list[str] | None = None\n    modifiers: list[dict[str, object]] | None = None\n    constraints: list[dict[str, object]] | None = None\n    materials: list[str | None] | None = None\n    visibility: dict[str, bool] | None = None\n    data_name: str | None = None\n    collections: list[str] | None = None\n    message: str | None = None\n\n\ndef main(params: Params) -> Result:\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    obj = bpy.data.objects.get(params.name)\n    if obj is None:\n        available = sorted(bpy.data.objects.keys())\n        return Result(\n            status=\"error\",\n            message=\"Object {!r} not found. Available objects: {:s}\".format(\n                params.name, \", \".join(available) if available else \"(none)\",\n            ),\n        )\n\n    return Result(\n        status=\"ok\",\n        name=obj.name,\n        type=obj.type,\n        location=list(obj.location),\n        rotation=list(obj.rotation_euler),\n        scale=list(obj.scale),\n        dimensions=list(obj.dimensions),\n        parent=obj.parent.name if obj.parent else None,\n        children=[child.name for child in obj.children],\n        modifiers=[\n            {\n                \"name\": mod.name,\n                \"type\": mod.type,\n                \"show_viewport\": mod.show_viewport,\n                \"show_render\": mod.show_render,\n            }\n            for mod in obj.modifiers\n        ],\n        constraints=[\n            {\n                \"name\": con.name,\n                \"type\": con.type,\n                \"enabled\": con.enabled,\n            }\n            for con in obj.constraints\n        ],\n        materials=[\n            slot.material.name if slot.material else None\n            for slot in obj.material_slots\n        ],\n        visibility={\n            \"hide_viewport\": obj.hide_viewport,\n            \"hide_render\": obj.hide_render,\n            \"hide_get\": obj.hide_get(),\n        },\n        data_name=obj.data.name if obj.data else None,\n        collections=[col.name for col in obj.users_collection],\n    )"

// blenderToolCodeGetBlendfileSummaryDatablocks is the Blender-side program for the get_blendfile_summary_datablocks tool.
const blenderToolCodeGetBlendfileSummaryDatablocks = "__all__ = (\n    \"Result\",\n    \"main\",\n)\n\nfrom typing import NamedTuple\n\n\nclass Result(NamedTuple):\n    status: str\n    datablock_counts: dict[str, int]\n    render_engine: str\n    scene_name: str\n    workspaces: list[str]\n    active_workspace: str | None = None\n\n\ndef main(params: None) -> Result:\n    del params\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    counts: dict[str, int] = {}\n    for attr in sorted(dir(bpy.data)):\n        val = getattr(bpy.data, attr, None)\n        if hasattr(val, \"__len__\") and hasattr(val, \"keys\"):\n            try:\n                n = len(val)  # type: ignore[arg-type]\n                if n > 0:\n                    counts[attr] = n\n            except Exception:  # pylint: disable=broad-exception-caught\n                # Some `bpy.data` attributes look like collections but\n                # raise when accessed (e.g. during undo). Safe to skip.\n                pass\n\n    window = getattr(bpy.context, \"window\", None)\n\n    return Result(\n        status=\"ok\",\n        datablock_counts=counts,\n        render_engine=bpy.context.scene.render.engine,\n        scene_name=bpy.context.scene.name,\n        workspaces=[ws.name for ws in bpy.data.workspaces],\n        active_workspace=window.workspace.name if window else None,\n    )"

// blenderToolCodeGetBlendfileSummaryPathInfo is the Blender-side program for the get_blendfile_summary_path_info tool.
const blenderToolCodeGetBlendfileSummaryPathInfo = "__all__ = (\n    \"Result\",\n    \"main\",\n)\n\nimport os\nimport time\nfrom typing import NamedTuple\n\n\n# Blender appends a number (1-32) to the file path for each backup.\n_MAX_BACKUPS = 32\n\n\nclass Result(NamedTuple):\n    status: str\n    filepath: str\n    is_saved: bool\n    is_dirty: bool\n    age_seconds: float | None = None\n    file_size_bytes: int | None = None\n    backups: list[dict[str, object]] | None = None\n\n\ndef main(params: None) -> Result:\n    del params\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    filepath = bpy.data.filepath\n    age = None\n    size = None\n    backups = None\n\n    if filepath and os.path.exists(filepath):\n        stat = os.stat(filepath)\n        age = round(time.time() - stat.st_mtime, 1)\n        size = stat.st_size\n\n        backups = []\n        for i in range(1, _MAX_BACKUPS + 1):\n            backup_path = filepath + str(i)\n            if not os.path.exists(backup_path):\n                break\n            bstat = os.stat(backup_path)\n            backups.append({\n                \"path\": backup_path,\n                \"age_seconds\": round(time.time() - bstat.st_mtime, 1),\n                \"size_bytes\": bstat.st_size,\n            })\n\n    return Result(\n        status=\"ok\",\n        filepath=filepath,\n        is_saved=bool(filepath),\n        is_dirty=bpy.data.is_dirty,\n        age_seconds=age,\n        file_size_bytes=size,\n        backups=backups,\n    )"

// blenderToolCodeGetBlendfileSummaryMissingFiles is the Blender-side program for the get_blendfile_summary_missing_files tool.
const blenderToolCodeGetBlendfileSummaryMissingFiles = "__all__ = (\n    \"Result\",\n    \"main\",\n)\n\nimport os\nfrom typing import NamedTuple\n\n\nclass Result(NamedTuple):\n    status: str\n    missing_files: list[dict[str, str]]\n    total_checked: int\n\n\ndef main(params: None) -> Result:\n    del params\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    missing: list[dict[str, str]] = []\n    checked = 0\n\n    def _visit(id_data: object, path: str, _placeholder: object) -> None:\n        nonlocal checked\n        checked += 1\n        filepath = bpy.path.abspath(path)\n        if not os.path.exists(filepath):\n            missing.append({\n                \"id_type\": type(id_data).__name__,\n                \"id_name\": getattr(id_data, \"name\", \"\"),\n                \"path\": filepath,\n            })\n\n    bpy.data.file_path_foreach(\n        _visit,\n        flags={\"SKIP_PACKED\", \"SKIP_WEAK_REFERENCES\", \"RESOLVE_TOKEN\"},\n    )\n\n    return Result(status=\"ok\", missing_files=missing, total_checked=checked)"

// blenderToolCodeGetBlendfileSummaryOfLinkedLibraries is the Blender-side program for the get_blendfile_summary_of_linked_libraries tool.
const blenderToolCodeGetBlendfileSummaryOfLinkedLibraries = "__all__ = (\n    \"Result\",\n    \"main\",\n)\n\nfrom typing import NamedTuple\n\n\nclass Result(NamedTuple):\n    status: str\n    direct_libraries: list[dict[str, object]]\n    indirect_libraries: list[dict[str, object]]\n    total_library_count: int\n\n\ndef main(params: None) -> Result:\n    del params\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    direct: list[dict[str, object]] = []\n    indirect: list[dict[str, object]] = []\n\n    for lib in bpy.data.libraries:\n        info: dict[str, object] = {\n            \"filepath\": lib.filepath,\n            \"name\": lib.name,\n        }\n\n        count = 0\n        for attr in dir(bpy.data):\n            collection = getattr(bpy.data, attr, None)\n            if not hasattr(collection, \"__iter__\"):\n                continue\n            try:\n                for item in collection:  # type: ignore[union-attr]\n                    if hasattr(item, \"library\") and item.library == lib:\n                        count += 1\n            except Exception:  # pylint: disable=broad-exception-caught\n                # Some `bpy.data` attributes look iterable but raise\n                # when traversed. Safe to skip for counting purposes.\n                pass\n        info[\"linked_datablocks_count\"] = count\n\n        if lib.parent is None:\n            direct.append(info)\n        else:\n            info[\"parent_library\"] = lib.parent.name\n            indirect.append(info)\n\n    return Result(\n        status=\"ok\",\n        direct_libraries=direct,\n        indirect_libraries=indirect,\n        total_library_count=len(bpy.data.libraries),\n    )"

// blenderToolCodeGetScreenshotOfWindowAsJson is the Blender-side program for the get_screenshot_of_window_as_json tool.
const blenderToolCodeGetScreenshotOfWindowAsJson = "__all__ = (\n    \"Result\",\n    \"main\",\n)\n\nfrom typing import NamedTuple\n\n\nclass Result(NamedTuple):\n    status: str\n    window_width: int\n    window_height: int\n    screen_name: str\n    workspace: str\n    scene: str\n    areas: list[dict[str, object]]\n    active_object: dict[str, object] | None\n    selected_objects: list[dict[str, str]]\n    message: str | None = None\n\n\ndef main(params: None) -> Result:\n    del params\n    import bpy  # pylint: disable=import-error,no-name-in-module\n    from bpy import context  # pylint: disable=import-error,no-name-in-module\n\n    error: str | None = None\n    window = context.window\n    if bpy.app.background:\n        error = \"Window layout is not available in background mode\"\n    elif window is None:\n        error = \"No active window\"\n\n    if error is not None:\n        return Result(\n            status=\"error\",\n            window_width=0,\n            window_height=0,\n            screen_name=\"\",\n            workspace=\"\",\n            scene=\"\",\n            areas=[],\n            active_object=None,\n            selected_objects=[],\n            message=error,\n        )\n    screen = window.screen\n\n    areas: list[dict[str, object]] = []\n    for area in screen.areas:\n        area_info: dict[str, object] = {\n            \"type\": area.type,\n            \"x\": area.x,\n            \"y\": area.y,\n            \"width\": area.width,\n            \"height\": area.height,\n        }\n\n        space = area.spaces.active\n        if space:\n            space_info: dict[str, object] = {\"type\": space.type}\n\n            if space.type == \"VIEW_3D\":\n                r3d = space.region_3d\n                if r3d:\n                    space_info[\"view_perspective\"] = r3d.view_perspective\n                    space_info[\"view_location\"] = list(r3d.view_location)\n                if hasattr(space, \"shading\"):\n                    space_info[\"shading_type\"] = space.shading.type\n                space_info[\"show_overlays\"] = space.overlay.show_overlays\n\n            elif space.type == \"PROPERTIES\":\n                space_info[\"context\"] = space.context\n\n            elif space.type == \"OUTLINER\":\n                space_info[\"display_mode\"] = space.display_mode\n\n            elif space.type == \"TEXT_EDITOR\":\n                if space.text:\n                    space_info[\"text_name\"] = space.text.name\n\n            elif space.type == \"NODE_EDITOR\":\n                space_info[\"tree_type\"] = space.tree_type\n                if space.node_tree:\n                    space_info[\"node_tree_name\"] = space.node_tree.name\n\n            area_info[\"space\"] = space_info\n\n        regions: list[dict[str, object]] = []\n        for region in area.regions:\n            if region.width > 0 and region.height > 0:\n                regions.append({\n                    \"type\": region.type,\n                    \"x\": region.x,\n                    \"y\": region.y,\n                    \"width\": region.width,\n                    \"height\": region.height,\n                })\n        area_info[\"regions\"] = regions\n        areas.append(area_info)\n\n    active = context.active_object\n    active_info = None\n    if active:\n        active_info = {\n            \"name\": active.name,\n            \"type\": active.type,\n            \"mode\": context.mode,\n            \"location\": list(active.location),\n        }\n\n    selected = [{\"name\": obj.name, \"type\": obj.type} for obj in context.selected_objects]\n\n    return Result(\n        status=\"ok\",\n        window_width=window.width,\n        window_height=window.height,\n        screen_name=screen.name,\n        workspace=window.workspace.name,\n        scene=context.scene.name,\n        areas=areas,\n        active_object=active_info,\n        selected_objects=selected,\n    )"

// blenderToolCodeGetScreenshotOfWindowAsImage is the Blender-side program for the get_screenshot_of_window_as_image tool.
const blenderToolCodeGetScreenshotOfWindowAsImage = "__all__ = (\n    \"Params\",\n    \"Result\",\n    \"main\",\n)\n\nimport base64\nimport os\nimport tempfile\nfrom typing import NamedTuple\n\n# MCP messages are limited to 1,048,576 bytes (1 MB). The image is base64-encoded\n# which expands data by 4/3. A typical full-resolution PNG screenshot exceeds\n# this, so we downscale iteratively until it fits.\n# Subtract 2048 for 2 KiB headroom for JSON envelope.\n_IMAGE_SIZE_LIMIT_IN_BYTES = ((1_048_576 - 2048) * 3) // 4\n\n\nclass Params(NamedTuple):\n    size_limit_in_bytes: int = 0\n\n\nclass Result(NamedTuple):\n    status: str\n    image_base64: str | None = None\n    message: str | None = None\n\n\n# SPDX-FileCopyrightText: 2026 Blender Authors\n#\n# SPDX-License-Identifier: GPL-3.0-or-later\n\n# Inline `_image_downscale_to_size_limit`.\n\n__all__ = ()\n\n\ndef _image_downscale_to_size_limit(\n        tmpdir: str, filepath: str,\n        size_limit_in_bytes: int,\n        size_tolerance_in_bytes: int = 0,\n) -> bytes:\n    \"\"\"\n    Downscale *filepath* for HiDPI and to stay under *size_limit_in_bytes*.\n\n    *size_tolerance_in_bytes* defines an acceptable margin below the limit.\n    Once a result fits within ``size_limit - tolerance``, the search stops\n    instead of continuing to find the least-downscaled option.\n\n    Returns the image file contents as bytes.\n    \"\"\"\n    import os\n\n    # Early out: if the file already fits, no processing needed.\n    if os.path.getsize(filepath) <= size_limit_in_bytes:\n        with open(filepath, \"rb\") as fh:\n            return fh.read()\n\n    import imbuf  # type: ignore[import-not-found]  # pylint: disable=import-error,no-name-in-module\n    from bpy import context  # pylint: disable=import-error,no-name-in-module\n\n    filepath_out = os.path.join(tmpdir, \"downscaled.png\")\n    im = imbuf.load(filepath)\n\n    # Downscale HiDPI images to logical pixel size.\n    pixel_size = context.preferences.system.pixel_size\n    if pixel_size > 1.0:\n        w, h = im.size\n        im.resize((round(w / pixel_size), round(h / pixel_size)), method='BILINEAR')\n\n    def _write_and_read(im_buf: \"imbuf.types.ImBuf\") -> bytes:\n        imbuf.write(im_buf, filepath=filepath_out)\n        with open(filepath_out, \"rb\") as fh:\n            return fh.read()\n\n    def _encode_at_divisor(divisor: int) -> bytes | None:\n        new_w = orig_w // divisor\n        new_h = orig_h // divisor\n        if new_w <= 64 or new_h <= 64:\n            return None\n        im_copy = im.copy()\n        im_copy.resize((new_w, new_h), method='BILINEAR')\n        result = _write_and_read(im_copy)\n        im_copy.free()\n        return result\n\n    try:\n        # Re-encode after HiDPI downscale (if applied) and check size.\n        data = _write_and_read(im)\n        if len(data) <= size_limit_in_bytes:\n            return data\n\n        # Dimension divisors: powers of 2 interleaved with 3x powers of 2.\n        # Gives: 1/2, 1/3, 1/4, 1/6, 1/8, 1/12, ... of the original.\n        divisors = (2, 3, 4, 6, 8, 12, 16, 24, 32, 48, 64)\n\n        orig_w, orig_h = im.size\n\n        # Trim divisors that would push dimensions below 64px.\n        max_idx = -1\n        for i, d in enumerate(divisors):\n            if orig_w // d > 64 and orig_h // d > 64:\n                max_idx = i\n            else:\n                break\n\n        if max_idx < 0:\n            return data\n\n        # Binary search for the lowest divisor whose output fits.\n        # Each probe estimates the next midpoint from the actual encoded size.\n        # PNG size scales roughly with pixel count (1/divisor^2),\n        # so: needed_divisor ~= current_divisor * sqrt(current_size / limit).\n        # Falls back to standard bisection when the range is too small.\n        import math\n        lo, hi = 0, max_idx\n        # Initial estimate uses divisor=1 (the full-size encode above).\n        estimated = math.sqrt(len(data) / size_limit_in_bytes)\n        while lo <= hi:\n            if hi - lo >= 2:\n                mid = min(hi - 1, max(lo + 1, min(\n                    range(lo, hi + 1),\n                    key=lambda i: abs(divisors[i] - estimated),\n                )))\n            else:\n                mid = (lo + hi) // 2\n            test_filedata_as_bytes = _encode_at_divisor(divisors[mid])\n            if test_filedata_as_bytes is not None and len(test_filedata_as_bytes) <= size_limit_in_bytes:\n                data = test_filedata_as_bytes\n                if len(test_filedata_as_bytes) == size_limit_in_bytes:\n                    break  # Exact fit, no better option possible.\n                if size_tolerance_in_bytes > 0:\n                    if len(test_filedata_as_bytes) < size_limit_in_bytes:\n                        if len(test_filedata_as_bytes) >= size_limit_in_bytes - size_tolerance_in_bytes:\n                            break  # Smaller but not by much, good enough.\n                hi = mid - 1\n                # Re-estimate: this divisor was enough, maybe a smaller one works.\n                estimated = divisors[mid] * math.sqrt(len(test_filedata_as_bytes) / size_limit_in_bytes)\n            else:\n                lo = mid + 1\n                # Re-estimate: need more scaling.\n                if test_filedata_as_bytes is not None:\n                    estimated = divisors[mid] * math.sqrt(len(test_filedata_as_bytes) / size_limit_in_bytes)\n\n    finally:\n        im.free()\n\n    return data\n\n\ndef main(params: Params) -> Result:\n    import bpy  # pylint: disable=import-error,no-name-in-module\n    from bpy import context  # pylint: disable=import-error,no-name-in-module\n\n    if bpy.app.background:\n        return Result(status=\"error\", message=\"Screenshots are not available in background mode\")\n\n    window = context.window\n    if window is None:\n        return Result(status=\"error\", message=\"No active window\")\n\n    size_limit = params.size_limit_in_bytes if params.size_limit_in_bytes > 0 else _IMAGE_SIZE_LIMIT_IN_BYTES\n\n    with tempfile.TemporaryDirectory(prefix=\"blmcp_screenshot_\") as tmpdir:\n        filepath_screenshot = os.path.join(tmpdir, \"screenshot.png\")\n        try:\n            bpy.ops.screen.screenshot(filepath=filepath_screenshot)\n        except RuntimeError as ex:\n            return Result(status=\"error\", message=str(ex))\n\n        image_data = _image_downscale_to_size_limit(\n            tmpdir, filepath_screenshot,\n            size_limit_in_bytes=size_limit,\n            size_tolerance_in_bytes=size_limit // 16,\n        )\n        data = base64.b64encode(image_data).decode(\"ascii\")\n\n    return Result(status=\"ok\", image_base64=data)"

// blenderToolCodeGetScreenshotOfAreaAsImage is the Blender-side program for the get_screenshot_of_area_as_image tool.
const blenderToolCodeGetScreenshotOfAreaAsImage = "__all__ = (\n    \"AreaUIType\",\n    \"Params\",\n    \"Result\",\n    \"main\",\n)\n\nimport base64\nimport os\nimport tempfile\nfrom typing import Literal, NamedTuple\n\n# MCP messages are limited to 1,048,576 bytes (1 MB). The image is base64-encoded\n# which expands data by 4/3. A typical full-resolution PNG screenshot exceeds\n# this, so we downscale iteratively until it fits.\n# Subtract 2048 for 2 KiB headroom for JSON envelope.\n_IMAGE_SIZE_LIMIT_IN_BYTES = ((1_048_576 - 2048) * 3) // 4\n\n# Rarely changes, regenerate with:\n# `list(bpy.types.Area.bl_rna.properties[\"ui_type\"].enum_items.keys())`\nAreaUIType = Literal[\n    # \"EMPTY\",  # Part of the enum but never visible to users.\n    \"VIEW_3D\",\n    \"IMAGE_EDITOR\",\n    \"UV\",\n    \"ShaderNodeTree\",\n    \"CompositorNodeTree\",\n    \"GeometryNodeTree\",\n    \"TextureNodeTree\",\n    \"SEQUENCE_EDITOR\",\n    \"CLIP_EDITOR\",\n    \"DOPESHEET_EDITOR\",\n    \"GRAPH_EDITOR\",\n    \"NLA_EDITOR\",\n    \"TEXT_EDITOR\",\n    \"CONSOLE\",\n    \"INFO\",\n    \"TOPBAR\",\n    \"STATUSBAR\",\n    \"OUTLINER\",\n    \"PROPERTIES\",\n    \"FILE_BROWSER\",\n    \"SPREADSHEET\",\n    \"PREFERENCES\",\n]\n\n\nclass Params(NamedTuple):\n    area_ui_type: AreaUIType\n    size_limit_in_bytes: int = 0\n\n\nclass Result(NamedTuple):\n    status: str\n    image_base64: str | None = None\n    message: str | None = None\n\n\n# SPDX-FileCopyrightText: 2026 Blender Authors\n#\n# SPDX-License-Identifier: GPL-3.0-or-later\n\n# Inline `_image_downscale_to_size_limit`.\n\n__all__ = ()\n\n\ndef _image_downscale_to_size_limit(\n        tmpdir: str, filepath: str,\n        size_limit_in_bytes: int,\n        size_tolerance_in_bytes: int = 0,\n) -> bytes:\n    \"\"\"\n    Downscale *filepath* for HiDPI and to stay under *size_limit_in_bytes*.\n\n    *size_tolerance_in_bytes* defines an acceptable margin below the limit.\n    Once a result fits within ``size_limit - tolerance``, the search stops\n    instead of continuing to find the least-downscaled option.\n\n    Returns the image file contents as bytes.\n    \"\"\"\n    import os\n\n    # Early out: if the file already fits, no processing needed.\n    if os.path.getsize(filepath) <= size_limit_in_bytes:\n        with open(filepath, \"rb\") as fh:\n            return fh.read()\n\n    import imbuf  # type: ignore[import-not-found]  # pylint: disable=import-error,no-name-in-module\n    from bpy import context  # pylint: disable=import-error,no-name-in-module\n\n    filepath_out = os.path.join(tmpdir, \"downscaled.png\")\n    im = imbuf.load(filepath)\n\n    # Downscale HiDPI images to logical pixel size.\n    pixel_size = context.preferences.system.pixel_size\n    if pixel_size > 1.0:\n        w, h = im.size\n        im.resize((round(w / pixel_size), round(h / pixel_size)), method='BILINEAR')\n\n    def _write_and_read(im_buf: \"imbuf.types.ImBuf\") -> bytes:\n        imbuf.write(im_buf, filepath=filepath_out)\n        with open(filepath_out, \"rb\") as fh:\n            return fh.read()\n\n    def _encode_at_divisor(divisor: int) -> bytes | None:\n        new_w = orig_w // divisor\n        new_h = orig_h // divisor\n        if new_w <= 64 or new_h <= 64:\n            return None\n        im_copy = im.copy()\n        im_copy.resize((new_w, new_h), method='BILINEAR')\n        result = _write_and_read(im_copy)\n        im_copy.free()\n        return result\n\n    try:\n        # Re-encode after HiDPI downscale (if applied) and check size.\n        data = _write_and_read(im)\n        if len(data) <= size_limit_in_bytes:\n            return data\n\n        # Dimension divisors: powers of 2 interleaved with 3x powers of 2.\n        # Gives: 1/2, 1/3, 1/4, 1/6, 1/8, 1/12, ... of the original.\n        divisors = (2, 3, 4, 6, 8, 12, 16, 24, 32, 48, 64)\n\n        orig_w, orig_h = im.size\n\n        # Trim divisors that would push dimensions below 64px.\n        max_idx = -1\n        for i, d in enumerate(divisors):\n            if orig_w // d > 64 and orig_h // d > 64:\n                max_idx = i\n            else:\n                break\n\n        if max_idx < 0:\n            return data\n\n        # Binary search for the lowest divisor whose output fits.\n        # Each probe estimates the next midpoint from the actual encoded size.\n        # PNG size scales roughly with pixel count (1/divisor^2),\n        # so: needed_divisor ~= current_divisor * sqrt(current_size / limit).\n        # Falls back to standard bisection when the range is too small.\n        import math\n        lo, hi = 0, max_idx\n        # Initial estimate uses divisor=1 (the full-size encode above).\n        estimated = math.sqrt(len(data) / size_limit_in_bytes)\n        while lo <= hi:\n            if hi - lo >= 2:\n                mid = min(hi - 1, max(lo + 1, min(\n                    range(lo, hi + 1),\n                    key=lambda i: abs(divisors[i] - estimated),\n                )))\n            else:\n                mid = (lo + hi) // 2\n            test_filedata_as_bytes = _encode_at_divisor(divisors[mid])\n            if test_filedata_as_bytes is not None and len(test_filedata_as_bytes) <= size_limit_in_bytes:\n                data = test_filedata_as_bytes\n                if len(test_filedata_as_bytes) == size_limit_in_bytes:\n                    break  # Exact fit, no better option possible.\n                if size_tolerance_in_bytes > 0:\n                    if len(test_filedata_as_bytes) < size_limit_in_bytes:\n                        if len(test_filedata_as_bytes) >= size_limit_in_bytes - size_tolerance_in_bytes:\n                            break  # Smaller but not by much, good enough.\n                hi = mid - 1\n                # Re-estimate: this divisor was enough, maybe a smaller one works.\n                estimated = divisors[mid] * math.sqrt(len(test_filedata_as_bytes) / size_limit_in_bytes)\n            else:\n                lo = mid + 1\n                # Re-estimate: need more scaling.\n                if test_filedata_as_bytes is not None:\n                    estimated = divisors[mid] * math.sqrt(len(test_filedata_as_bytes) / size_limit_in_bytes)\n\n    finally:\n        im.free()\n\n    return data\n\n\ndef main(params: Params) -> Result:\n    import bpy  # pylint: disable=import-error,no-name-in-module\n    from bpy import context  # pylint: disable=import-error,no-name-in-module\n\n    if bpy.app.background:\n        return Result(status=\"error\", message=\"Screenshots are not available in background mode\")\n\n    window = context.window\n    if window is None:\n        return Result(status=\"error\", message=\"No active window\")\n    screen = window.screen\n\n    # Prefer the context's active area if it matches,\n    # otherwise pick the largest matching area.\n    area = context.area\n    if area is None or area.ui_type != params.area_ui_type:\n        area = next(iter(sorted(\n            (a for a in screen.areas if a.ui_type == params.area_ui_type),\n            key=lambda a: -(a.width * a.height),\n        )), None)\n    if area is None:\n        available = sorted({a.ui_type for a in screen.areas})\n        return Result(\n            status=\"error\",\n            message=\"No area with type {!r} found. Available: {:s}\".format(\n                params.area_ui_type, \", \".join(available),\n            ),\n        )\n\n    size_limit = params.size_limit_in_bytes if params.size_limit_in_bytes > 0 else _IMAGE_SIZE_LIMIT_IN_BYTES\n\n    with tempfile.TemporaryDirectory(prefix=\"blmcp_screenshot_\") as tmpdir:\n        filepath_screenshot = os.path.join(tmpdir, \"screenshot.png\")\n        with context.temp_override(window=window, area=area):\n            try:\n                bpy.ops.screen.screenshot_area(filepath=filepath_screenshot)\n            except RuntimeError as ex:\n                return Result(status=\"error\", message=str(ex))\n\n        image_data = _image_downscale_to_size_limit(\n            tmpdir, filepath_screenshot,\n            size_limit_in_bytes=size_limit,\n            size_tolerance_in_bytes=size_limit // 16,\n        )\n        data = base64.b64encode(image_data).decode(\"ascii\")\n\n    return Result(status=\"ok\", image_base64=data)"

// blenderToolCodeJumpToTabByName is the Blender-side program for the jump_to_tab_by_name tool.
const blenderToolCodeJumpToTabByName = "__all__ = (\n    \"Params\",\n    \"Result\",\n    \"main\",\n)\n\nfrom typing import NamedTuple\n\n\nclass Params(NamedTuple):\n    name: str\n\n\nclass Result(NamedTuple):\n    status: str\n    workspace: str | None = None\n    message: str | None = None\n    available_workspaces: list[str] | None = None\n\n\ndef main(params: Params) -> Result:\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    if bpy.app.background:\n        return Result(status=\"error\", message=\"Not available in background mode\")\n    if bpy.context.window is None:\n        return Result(status=\"error\", message=\"No active window\")\n\n    ws = bpy.data.workspaces.get(params.name)\n    if ws is None:\n        return Result(\n            status=\"error\",\n            message=\"Workspace {!r} not found\".format(params.name),\n            available_workspaces=[w.name for w in bpy.data.workspaces],\n        )\n\n    bpy.context.window.workspace = ws\n    return Result(status=\"ok\", workspace=ws.name)"

// blenderToolCodeJumpToTabBySpaceType is the Blender-side program for the jump_to_tab_by_space_type tool.
const blenderToolCodeJumpToTabBySpaceType = "__all__ = (\n    \"Params\",\n    \"Result\",\n    \"main\",\n)\n\nfrom typing import NamedTuple\n\n\nclass Params(NamedTuple):\n    space_type: str\n    allow_edits: bool\n\n\nclass Result(NamedTuple):\n    status: str\n    workspace: str | None = None\n    space_type: str | None = None\n    created: bool | None = None\n    message: str | None = None\n    available_space_types: list[str] | None = None\n\n\ndef main(params: Params) -> Result:\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    if bpy.app.background:\n        return Result(status=\"error\", message=\"Not available in background mode\")\n    if bpy.context.window is None:\n        return Result(status=\"error\", message=\"No active window\")\n\n    def _largest_area(screen: \"bpy.types.Screen\") -> \"bpy.types.Area | None\":\n        return max(screen.areas, key=lambda a: a.width * a.height, default=None)\n\n    # Find an existing workspace whose largest area matches the desired space type.\n    found = None\n    for ws in bpy.data.workspaces:\n        for screen in ws.screens:\n            area = _largest_area(screen)\n            if area is not None and area.type == params.space_type:\n                found = ws\n                break\n        if found:\n            break\n\n    if found:\n        bpy.context.window.workspace = found\n        return Result(status=\"ok\", workspace=found.name, space_type=params.space_type)\n\n    if params.allow_edits:\n        # Duplicate the current workspace and change its main area type.\n        try:\n            bpy.ops.workspace.duplicate()\n        except RuntimeError as ex:\n            return Result(status=\"error\", message=str(ex))\n        new_ws = bpy.context.window.workspace\n        new_ws.name = params.space_type.replace(\"_\", \" \").title()\n        area = _largest_area(bpy.context.screen)\n        if area is not None:\n            area.type = params.space_type\n        return Result(\n            status=\"ok\",\n            workspace=new_ws.name,\n            space_type=params.space_type,\n            created=True,\n        )\n\n    available = sorted({\n        area.type\n        for ws in bpy.data.workspaces\n        for screen in ws.screens\n        for area in ((_largest_area(screen),) if _largest_area(screen) else ())\n        if area is not None\n    })\n    return Result(\n        status=\"error\",\n        message=\"No workspace with space type {!r} found\".format(params.space_type),\n        available_space_types=available,\n    )"

// blenderToolCodeJumpToView3dObjectByName is the Blender-side program for the jump_to_view3d_object_by_name tool.
const blenderToolCodeJumpToView3dObjectByName = "__all__ = (\n    \"Params\",\n    \"Result\",\n    \"main\",\n)\n\nfrom typing import Any, NamedTuple\n\n\nclass Params(NamedTuple):\n    name: str\n    allow_edits: bool\n\n\nclass Result(NamedTuple):\n    status: str\n    object: str | None = None\n    type: str | None = None\n    location: list[float] | None = None\n    message: str | None = None\n\n\ndef main(params: Params) -> Result:\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    if bpy.app.background:\n        return Result(status=\"error\", message=\"Not available in background mode\")\n    if bpy.context.window is None:\n        return Result(status=\"error\", message=\"No active window\")\n\n    obj = bpy.data.objects.get(params.name)\n    if obj is None:\n        return Result(status=\"error\", message=\"Object {!r} not found\".format(params.name))\n\n    if params.allow_edits:\n        if obj.hide_viewport:\n            obj.hide_viewport = False\n        if obj.hide_get():\n            obj.hide_set(False)\n\n        def _enable_collections(layer_col: Any, target: Any) -> bool:\n            found = False\n            for child in layer_col.children:\n                if _enable_collections(child, target):\n                    found = True\n            if target.name in layer_col.collection.objects:\n                found = True\n            if found:\n                layer_col.exclude = False\n                layer_col.hide_viewport = False\n            return found\n\n        _enable_collections(bpy.context.view_layer.layer_collection, obj)\n\n    if bpy.context.mode != \"OBJECT\":\n        bpy.ops.object.mode_set(mode=\"OBJECT\")\n\n    bpy.ops.object.select_all(action=\"DESELECT\")\n    obj.select_set(True)\n    bpy.context.view_layer.objects.active = obj\n\n    view3d_found = False\n    for area in bpy.context.screen.areas:\n        if area.type == \"VIEW_3D\":\n            view3d_found = True\n            # Exit camera view so the user sees the object in a free viewport.\n            r3d = area.spaces.active.region_3d\n            if r3d and r3d.view_perspective == \"CAMERA\":\n                r3d.view_perspective = \"PERSP\"\n            for region in area.regions:\n                if region.type == \"WINDOW\":\n                    with bpy.context.temp_override(area=area, region=region):\n                        bpy.ops.view3d.view_selected()\n                    break\n            break\n\n    return Result(\n        status=\"ok\",\n        object=params.name,\n        type=obj.type,\n        location=list(obj.location),\n        message=None if view3d_found else \"No 3D viewport found, object selected but not framed\",\n    )"

// blenderToolCodeJumpToView3dObjectDataByName is the Blender-side program for the jump_to_view3d_object_data_by_name tool.
const blenderToolCodeJumpToView3dObjectDataByName = "__all__ = (\n    \"Params\",\n    \"Result\",\n    \"main\",\n)\n\nfrom typing import Any, NamedTuple\n\n\nclass Params(NamedTuple):\n    name: str\n    allow_edits: bool\n\n\nclass Result(NamedTuple):\n    status: str\n    object: str | None = None\n    data_name: str | None = None\n    type: str | None = None\n    location: list[float] | None = None\n    message: str | None = None\n\n\ndef main(params: Params) -> Result:\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    if bpy.app.background:\n        return Result(status=\"error\", message=\"Not available in background mode\")\n    if bpy.context.window is None:\n        return Result(status=\"error\", message=\"No active window\")\n\n    target = None\n    for obj in bpy.data.objects:\n        if obj.data is not None and obj.data.name == params.name:\n            target = obj\n            break\n    if target is None:\n        return Result(\n            status=\"error\",\n            message=\"No object found with data named {!r}\".format(params.name),\n        )\n\n    if params.allow_edits:\n        if target.hide_viewport:\n            target.hide_viewport = False\n        if target.hide_get():\n            target.hide_set(False)\n\n        def _enable_collections(layer_col: Any, tgt: Any) -> bool:\n            found = False\n            for child in layer_col.children:\n                if _enable_collections(child, tgt):\n                    found = True\n            if tgt.name in layer_col.collection.objects:\n                found = True\n            if found:\n                layer_col.exclude = False\n                layer_col.hide_viewport = False\n            return found\n\n        _enable_collections(bpy.context.view_layer.layer_collection, target)\n\n    if bpy.context.mode != \"OBJECT\":\n        bpy.ops.object.mode_set(mode=\"OBJECT\")\n\n    bpy.ops.object.select_all(action=\"DESELECT\")\n    target.select_set(True)\n    bpy.context.view_layer.objects.active = target\n\n    view3d_found = False\n    for area in bpy.context.screen.areas:\n        if area.type == \"VIEW_3D\":\n            view3d_found = True\n            # Exit camera view so the user sees the object in a free viewport.\n            r3d = area.spaces.active.region_3d\n            if r3d and r3d.view_perspective == \"CAMERA\":\n                r3d.view_perspective = \"PERSP\"\n            for region in area.regions:\n                if region.type == \"WINDOW\":\n                    with bpy.context.temp_override(area=area, region=region):\n                        bpy.ops.view3d.view_selected()\n                    break\n            break\n\n    return Result(\n        status=\"ok\",\n        object=target.name,\n        data_name=params.name,\n        type=target.type,\n        location=list(target.location),\n        message=None if view3d_found else \"No 3D viewport found, object selected but not framed\",\n    )"

// blenderToolCodeRenderViewportToPath is the Blender-side program for the render_viewport_to_path tool.
const blenderToolCodeRenderViewportToPath = "__all__ = (\n    \"Params\",\n    \"Result\",\n    \"main\",\n)\n\nfrom collections.abc import Callable\nfrom typing import NamedTuple\n\n\nclass Params(NamedTuple):\n    output_path: str\n\n\nclass Result(NamedTuple):\n    status: str\n    filepath: str | None = None\n    message: str | None = None\n\n\n# SPDX-FileCopyrightText: 2026 Blender Authors\n#\n# SPDX-License-Identifier: GPL-3.0-or-later\n\n# Inline `_deferred_tool_check_for_file_output`.\n\n__all__ = ()\n\nimport os\nfrom collections.abc import Callable\n\n\ndef _deferred_tool_check_for_file_output(\n        job_type: str,\n        output_path: str,\n        restore_attrs: list[tuple[object, str, object]] | None = None,\n) -> Callable[[], dict[str, object] | None]:\n    \"\"\"\n    Return a deferred checker for a background job that writes a file.\n\n    The returned callable follows the deferred tool convention\n    (see ``deferred_tool.py``): returns ``None`` while the job is\n    running, or a result ``dict`` once it completes.\n\n    When *restore_attrs* is provided, each ``(obj, attr, value)`` tuple\n    is restored via ``setattr`` after the job completes.\n    \"\"\"\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    def check_is_finished() -> dict[str, object] | None:\n        if bpy.app.is_job_running(job_type):\n            return None\n        if restore_attrs is not None:\n            for obj, attr, value in restore_attrs:\n                setattr(obj, attr, value)\n        if os.path.exists(output_path):\n            return {\"status\": \"ok\", \"filepath\": output_path}\n        return {\"status\": \"error\", \"message\": \"Job completed but output file was not created\"}\n\n    return check_is_finished\n\n\ndef main(params: Params) -> Result | Callable[[], dict[str, object] | None]:\n    import os\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    use_deferred = not bpy.app.background\n\n    # Resolve the output path inside the MCP scratch directory.\n    output_path = os.path.join(bpy.app.tempdir, \"blender_mcp\", os.path.basename(params.output_path))\n\n    scene = bpy.context.scene\n\n    # NOTE: `filepath` is set manually (not via context manager) because\n    # `write_still` reads it after the render completes.\n    # With `INVOKE_DEFAULT` a context manager would restore it too early.\n    rd = scene.render\n    orig_filepath = rd.filepath\n    rd.filepath = output_path\n\n    render_args = ('INVOKE_DEFAULT',) if use_deferred else ()\n\n    try:\n        bpy.ops.render.render(*render_args, write_still=True)\n    except RuntimeError as ex:\n        rd.filepath = orig_filepath\n        return Result(status=\"error\", message=str(ex))\n\n    if use_deferred:\n        return _deferred_tool_check_for_file_output(\n            'RENDER', output_path, restore_attrs=[(rd, \"filepath\", orig_filepath)],\n        )\n\n    rd.filepath = orig_filepath\n    return Result(status=\"ok\", filepath=output_path)"

// blenderToolCodeRenderThumbnailToPath is the Blender-side program for the render_thumbnail_to_path tool.
const blenderToolCodeRenderThumbnailToPath = "__all__ = (\n    \"Params\",\n    \"Result\",\n    \"main\",\n)\n\nimport contextlib\nimport typing\nfrom collections.abc import Callable\nfrom typing import NamedTuple\n\n# Thumbnail render settings. Small resolution and low samples\n# for a fast preview that is still useful for visual inspection.\n# The longest dimension is clamped to this value, preserving aspect ratio.\n_THUMB_DIMS_MAX = 320\n_THUMB_SIMPLIFY_SUBDIV = 1\n_THUMB_CYCLES_SAMPLES = 16\n_THUMB_EEVEE_SAMPLES = 16\n\n\nclass Params(NamedTuple):\n    output_path: str\n\n\nclass Result(NamedTuple):\n    status: str\n    filepath: str | None = None\n    message: str | None = None\n\n\n# SPDX-FileCopyrightText: 2026 Blender Authors\n#\n# SPDX-License-Identifier: GPL-3.0-or-later\n\n# Inline `_backup_attrs_and_assign_multi`.\n\n__all__ = ()\n\nimport contextlib\nfrom typing import Generator\n\n\n@contextlib.contextmanager\ndef _backup_attrs_and_assign_multi(\n        *obj_attrs: tuple[object, dict[str, object]],\n) -> Generator[None, None, None]:\n    \"\"\"\n    Context manager that saves and assigns attributes on multiple objects,\n    restoring all originals on exit.\n\n    Each argument is a ``(obj, {attr: value, ...})`` pair.\n    \"\"\"\n    saved: list[tuple[object, dict[str, object]]] = []\n    try:\n        for obj, attrs in obj_attrs:\n            saved.append((obj, {attr: getattr(obj, attr) for attr in attrs}))\n            for attr, value in attrs.items():\n                setattr(obj, attr, value)\n        yield\n    finally:\n        for obj, attrs in saved:\n            for attr, value in attrs.items():\n                setattr(obj, attr, value)\n\n\n# SPDX-FileCopyrightText: 2026 Blender Authors\n#\n# SPDX-License-Identifier: GPL-3.0-or-later\n\n# Inline `_deferred_tool_check_for_file_output`.\n\n__all__ = ()\n\nimport os\nfrom collections.abc import Callable\n\n\ndef _deferred_tool_check_for_file_output(\n        job_type: str,\n        output_path: str,\n        restore_attrs: list[tuple[object, str, object]] | None = None,\n) -> Callable[[], dict[str, object] | None]:\n    \"\"\"\n    Return a deferred checker for a background job that writes a file.\n\n    The returned callable follows the deferred tool convention\n    (see ``deferred_tool.py``): returns ``None`` while the job is\n    running, or a result ``dict`` once it completes.\n\n    When *restore_attrs* is provided, each ``(obj, attr, value)`` tuple\n    is restored via ``setattr`` after the job completes.\n    \"\"\"\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    def check_is_finished() -> dict[str, object] | None:\n        if bpy.app.is_job_running(job_type):\n            return None\n        if restore_attrs is not None:\n            for obj, attr, value in restore_attrs:\n                setattr(obj, attr, value)\n        if os.path.exists(output_path):\n            return {\"status\": \"ok\", \"filepath\": output_path}\n        return {\"status\": \"error\", \"message\": \"Job completed but output file was not created\"}\n\n    return check_is_finished\n\n\ndef main(params: Params) -> Result | Callable[[], dict[str, object] | None]:\n    import os\n    import bpy  # pylint: disable=import-error,no-name-in-module\n\n    use_deferred = not bpy.app.background\n\n    # Resolve the output path inside the MCP scratch directory.\n    output_path = os.path.join(bpy.app.tempdir, \"blender_mcp\", os.path.basename(params.output_path))\n\n    scene = bpy.context.scene\n    rd = scene.render\n\n    # Compute thumbnail resolution from originals, preserving aspect ratio.\n    res_x = rd.resolution_x\n    res_y = rd.resolution_y\n    if res_x >= res_y:\n        thumb_x = _THUMB_DIMS_MAX\n        thumb_y = max(int(res_y * _THUMB_DIMS_MAX / res_x), 1)\n    else:\n        thumb_y = _THUMB_DIMS_MAX\n        thumb_x = max(int(res_x * _THUMB_DIMS_MAX / res_y), 1)\n\n    # NOTE: `filepath` is excluded from `_backup_attrs_and_assign_multi`\n    # because `write_still` reads it after the render completes. With\n    # `INVOKE_DEFAULT` the context manager would restore it before the\n    # file is written. Instead, filepath is saved/restored manually.\n    orig_filepath = rd.filepath\n    rd.filepath = output_path\n\n    obj_attrs: list[tuple[object, dict[str, object]]] = [\n        (rd, {\n            \"resolution_x\": thumb_x,\n            \"resolution_y\": thumb_y,\n            \"resolution_percentage\": 100,\n            \"use_simplify\": True,\n            \"simplify_subdivision_render\": _THUMB_SIMPLIFY_SUBDIV,\n        }),\n    ]\n    if rd.engine == \"CYCLES\":\n        obj_attrs.append((scene.cycles, {\"samples\": _THUMB_CYCLES_SAMPLES}))\n    elif rd.engine == \"BLENDER_EEVEE_NEXT\":\n        obj_attrs.append((scene.eevee, {\"taa_render_samples\": _THUMB_EEVEE_SAMPLES}))\n\n    render_args = ('INVOKE_DEFAULT',) if use_deferred else ()\n\n    with _backup_attrs_and_assign_multi(*obj_attrs):\n        try:\n            bpy.ops.render.render(*render_args, write_still=True)\n        except RuntimeError as ex:\n            rd.filepath = orig_filepath\n            return Result(status=\"error\", message=str(ex))\n\n    if use_deferred:\n        return _deferred_tool_check_for_file_output(\n            'RENDER', output_path, restore_attrs=[(rd, \"filepath\", orig_filepath)],\n        )\n\n    rd.filepath = orig_filepath\n    return Result(status=\"ok\", filepath=output_path)"
